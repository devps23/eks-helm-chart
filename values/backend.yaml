metadata:
  component: backend
  project_name: expense
  appVersion: "1.4"
initContainers:
  enabled: true
containers:
  name: backend
selector:
  appName: backend
  type: ClusterIP
ports:
  port: 80
  targetPort: 8080
dockerImage: "1.4"


